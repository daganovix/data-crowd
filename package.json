{
  "name": "data-crowd",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "node server/dist/index.js",
    "db:migrate": "npm run db:migrate:deploy --workspace=server",
    "setup": "npm install && npm run db:push --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
